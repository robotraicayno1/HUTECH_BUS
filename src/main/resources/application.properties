# Cấu hình ứng dụng HUTECHBUS
spring.application.name=HUTECHBUS
server.port=8080

# Kết nối MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/HUTECHBUS

# Tắt cache Thymeleaf để phát triển nhanh hơn
# Nên bật lại (true) trước khi triển khai production
spring.thymeleaf.cache=false
